{
  "private": true,
  "workspaces": {
    "packages": [
      "lib",
      "test",
      "web"
    ]
  },
  "scripts": {
    "dev": "yarn workspace @jacob-roberts/lighter watch & yarn workspace lighter-web dev",
    "changeset": "changeset",
    "version": "changeset version",
    "build": "yarn workspace @jacob-roberts/lighter build",
    "release": "yarn build && changeset publish"
  },
  "repository": "code-hike/lighter",
  "author": "pomber",
  "dependencies": {
    "@changesets/cli": "^2.26.0"
  }
}
